# pnpm workspace — code/ 아래 클라이언트 계열 패키지를 모은다.
# Rust 서버(src/, Cargo.toml)는 Cargo 관례대로 루트에 둔다(D1, grill.md).
#
# code/web 은 rust_embed 로 바이너리에 박히는 자산 폴더라 package.json 이 없다 —
# pnpm 은 package.json 없는 디렉터리를 조용히 건너뛴다. code/android 도 마찬가지.
packages:
  - "code/*"
