[versions]
# Build tooling — deliberately identical to the reference toolchain so the
# local Gradle cache (already holding these artifacts) is reused verbatim.
agp = "8.7.3"
kotlin = "2.1.0"

# QR scanning (T04). Pinned stable versions compatible with compileSdk 35 /
# AGP 8.7.3 / minSdk 29; fetched from the google() repository like everything
# else so no extra repository is added.
camerax = "1.3.4"
mlkitBarcode = "17.3.0"

[libraries]
# CameraX (QR scanner preview + analysis)
androidx-camera-core = { group = "androidx.camera", name = "camera-core", version.ref = "camerax" }
androidx-camera-camera2 = { group = "androidx.camera", name = "camera-camera2", version.ref = "camerax" }
androidx-camera-lifecycle = { group = "androidx.camera", name = "camera-lifecycle", version.ref = "camerax" }
androidx-camera-view = { group = "androidx.camera", name = "camera-view", version.ref = "camerax" }
# ML Kit bundled barcode model (on-device QR decoding, no Play Services needed)
mlkit-barcode-scanning = { group = "com.google.mlkit", name = "barcode-scanning", version.ref = "mlkitBarcode" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
